# Dependências do app principal (main.py).
# Instale com: pip install -r requirements.txt
flask>=3.0.0
langchain>=0.3.0
langchain-openai>=0.2.0
langgraph>=0.2.0
python-dotenv>=1.0.0
pillow>=10.4.0
requests>=2.31.0

# Opcional — necessário apenas para o provedor "Fine Tuning (Com Visão)",
# que roda o detector YOLO treinado dentro do processo Flask.
# ultralytics>=8.3.0
